import { store } from "../../app/store";
import { fetchLocation } from "./weatherSlice";

test("Fetch woeid location", async () => {
  /*let woeidLocation = store.getState().weather.woeidLocation;
  console.log(woeidLocation);
  store.dispatch(fetchLocation("Milan"));
  woeidLocation = store.getState().weather.woeidLocation;
  const result = await expect(woeidLocation).not.toBeNull();
  console.log(result);*/
});
